import { CustomtimeformatterPipe } from './customtimeformatter.pipe';

describe('CustomtimeformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomtimeformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
